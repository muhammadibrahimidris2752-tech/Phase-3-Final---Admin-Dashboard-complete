/**
 * Orders — server-side order creation and finalization.
 *
 * Migrated here from functions/src/orders (also still a placeholder
 * today) once this AWS proof-of-concept is validated. Empty for now —
 * do not implement until instructed.
 */
export {};
