/**
 * Email — transactional customer/admin notifications.
 *
 * Migrated here from functions/src/email (also still a placeholder
 * today) once this AWS proof-of-concept is validated. Empty for now —
 * do not implement until instructed.
 */
export {};
